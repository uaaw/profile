---
const projects = [
  {
    name: 'fluxer-zig',
    description: 'fluxer zigライブラリ',
    url: 'https://github.com/uaaw/fluxer-zig.git',
  },
  {
    name: 'purus',
    description: 'A modern programming language',
    url: 'https://github.com/puruslang/purus.git',
  },
  {
    name: 'fluxer',
    description: 'fluxer公式ウェブサイトの和訳監修',
    url: 'https://github.com/fluxerapp/fluxer.git',
  },
];
---

<section>
  <h2 class="section-title">Projects</h2>
  <div class="projects-grid">
    {
      projects.map((project) => (
        <a
          class="card"
          href={project.url}
          target="_blank"
          rel="noopener noreferrer"
        >
          <h3 class="project-name">{project.name}</h3>
          <p class="project-desc">{project.description}</p>
          <span class="project-link">{project.url}</span>
        </a>
      ))
    }
  </div>
</section>

<style>
  .section-title {
    font-size: 1.6rem;
    font-weight: 300;
    color: #1a1e2e;
    margin-bottom: 2rem;
    letter-spacing: -0.02em;
  }

  .projects-grid {
    display: grid;
    grid-template-columns: 1fr;
    gap: 1rem;
  }

  @media (min-width: 768px) {
    .projects-grid {
      grid-template-columns: repeat(3, 1fr);
    }
  }

  .card {
    display: flex;
    flex-direction: column;
    gap: 0.75rem;
    padding: 1.5rem;
    transition: border-color 0.2s, background 0.2s;
  }

  .project-name {
    font-weight: 500;
    font-size: 1.1rem;
    color: #1a1e2e;
  }

  .project-desc {
    font-size: 0.85rem;
    color: #3a4060;
    line-height: 1.5;
  }

  .project-link {
    font-size: 0.8rem;
    color: var(--accent);
    word-break: break-all;
  }
</style>
